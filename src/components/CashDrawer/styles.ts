import styled from "styled-components";
import { defaultThemes } from "../../styles/themes/default";

export const StyledCashDrawer = styled.div`
    background-color: ${defaultThemes["white-100"]};
    min-height: 20rem;
    width: 100%;
    border-radius: .4rem;
    margin-bottom: .5rem;
`;